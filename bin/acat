#!/bin/sh
# Run acatalogue from a checkout without installing it: ./bin/acat <command> ...
here=$(cd "$(dirname "$0")/.." && pwd)
PYTHONPATH="$here${PYTHONPATH:+:$PYTHONPATH}" exec python3 -m acatalogue "$@"
